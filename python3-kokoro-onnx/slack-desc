# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                   |-----handy-ruler------------------------------------------------------|
python3-kokoro-onnx: python3-kokoro-onnx (text to speech with Kokoro and onnxruntime)
python3-kokoro-onnx:
python3-kokoro-onnx: kokoro-onnx runs the Kokoro-82M text to speech model through
python3-kokoro-onnx: onnxruntime, with phonemizer and espeak-ng for phonemization. It
python3-kokoro-onnx: supports several languages. Model and voices are downloaded
python3-kokoro-onnx: separately, see the README.
python3-kokoro-onnx:
python3-kokoro-onnx: Homepage: https://github.com/thewh1teagle/kokoro-onnx
python3-kokoro-onnx:
python3-kokoro-onnx:
python3-kokoro-onnx:
