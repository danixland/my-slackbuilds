# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

                  |-----handy-ruler------------------------------------------------------|
python3-truststore: python3-truststore (certificate verification via system trust store)
python3-truststore:
python3-truststore: truststore verifies certificates using the operating system's
python3-truststore: native trust store instead of a bundled CA file.
python3-truststore:
python3-truststore: Homepage: https://github.com/sethmlarson/truststore
python3-truststore:
python3-truststore:
python3-truststore:
python3-truststore:
python3-truststore:
